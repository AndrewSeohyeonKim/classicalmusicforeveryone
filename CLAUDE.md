# CMFE 웹사이트 — 작업 지침

이 저장소는 **Classical Music for Everyone(CMFE)의 공개 웹사이트**다.
GitHub Pages 정적 사이트. 빌드 도구·npm 없음.

공개 주소: `https://andrewseohyeonkim.github.io/classicalmusicforeveryone/`

## 1. 브랜드 — 브랜드 B(CMFE)다. 아일랜드 동행(브랜드 A)과 섞지 않는다.

정본은 iCloud `00_최종본/06_Brand-Standards_EN.html`.

```
Ink Navy      #1D2430   로고 바탕색·본문
Gold Bronze   #B8893A   로고 금색·액센트
Light Gold    #D9B36A   반전 로고의 금색·어두운 면 위 강조
Warm Cream    #FAF5EE   배경
Soft Navy     #33405C   패널
Antique Gold  #B4914F   액센트
Dusty Rose    #8C4A56   강조
Slate         #5F6B7D   보조 텍스트
```

- 제목 **EB Garamond**, 본문 **Plus Jakarta Sans**, 한글 **Noto Sans KR**. 네 번째 서체를
  들이지 않는다.
- **Fraunces는 쓰지 않는다(2026-09-04, Andrew 결정).** 쐐기 세리프와 wonky 이탤릭이
  디스플레이 크기에서 산만하게 읽힌다는 판단이다. 정본 `06_Brand-Standards_EN`은 아직
  Fraunces로 적혀 있으므로 **정본도 갱신해야 한다**(§7).
- EB Garamond을 고른 이유는 로고 워드마크의 Cormorant Garamond과 같은 올드스타일
  계보라서다. 제목이 로고와 다투지 않는다. 자간은 Fraunces보다 훨씬 덜 조인다
  (`h1` −.012em). 서체를 다시 고를 일이 생기면 이 기준을 먼저 본다.
- Cormorant Garamond은 **로고 워드마크 전용**이다. 로고는 SVG로 쓰므로 따로 불러오지 않는다.
- 로고를 늘리거나 색을 바꾸거나 그림자를 넣지 않는다. 밝은 바탕 → `logo-horizontal.svg`,
  어두운 바탕 → `logo-reversed.svg`.
- `assets/`의 로고 계열은 다섯이다: `logo-horizontal` · `logo-reversed` · `logo-vertical` ·
  `logo-icon` · `logo-signature`(이메일 서명용 600×210 PNG, 투명도를 지우는 메일
  클라이언트를 위한 `logo-signature-white`와 한 쌍). 정본은
  `10_CMFE_로고·브랜드셋업/`이고 `이메일서명/` 하위에 서명용이 있다.
  **로고 마스터 PNG를 저장소에 또 넣지 않는다** — `logo-horizontal.png`가 같은 조합이다.
- 로고의 **의미**는 비례에 있다: `for`는 작게, `Everyone`은 크고 금색. 홈의 `.band-mark`와
  About `#identity`가 그 이유를 말한다. 이 두 곳의 문장을 바꿀 때는 서로 어긋나지 않게 한다.
- 팔레트·토큰은 `styles.css` 최상단 `:root`가 이 저장소의 정본이다.

## 2. 사실관계 — 정본이 이긴다

숫자·날짜·장소는 iCloud `프로젝트 전체 파일, 후원, 펀딩/00_최종본/`의 정본 문서를 따른다.
사이트와 정본이 어긋나면 **사이트를 고친다.**

특히 주의(정본 `03_Track-Record_EN` §6에 명시된 과거 오류):

- **참가자 나이를 쓰지 않는다.** ("78–92세" 등은 검증되지 않은 수치)
- 파일럿은 **은퇴 프레젠테이션 수녀 7명 · 10주 · 주 1회 1시간**. 8명·9명·42주가 아니다.
- 파일럿 합주 장소는 **Warrenmount(D8)**, 부활 음악회 장소는 **Clondalkin Lodge**. 둘을 섞지 않는다.
- Letters Ensemble 정식 음악회는 **4회**.
- "40+"의 구성: 강의 17 + 찾아가는 음악회 20 + 파일럿 음악회 1 + SDCC 2 = 40.
  Letters Ensemble 4회는 이미 20 안에 들어 있다. **다시 더하지 않는다.**
- 4개국 = 아일랜드·프랑스·영국·한국. 포르투갈·이탈리아는 창립자의 자원봉사지, 연주국이 아니다.
- **차량은 확보됐다(2026-09-04, Andrew 확인).** 개인 후원으로 마련했다. 홈 "후원이 이미
  바꾼 것" 섹션이 이 사실을 쓴다. 다만 **모금액·날짜·기여자 수는 아직 받지 못했으므로
  숫자를 지어 넣지 않는다.** 정본 `00_최종본`은 아직 "미확보"로 적혀 있어 갱신 대상이다(§7).
- **지지 서한의 범위를 넓히지 않는다.** Rua Red · The Civic · Tallaght University Hospital의
  서한은 **South Dublin Live 2026 신청 한 건을 위해** 받은 것이다. 단체 전체에 대한 포괄적
  보증처럼 쓰지 않는다. 후원 페이지의 목록 제목과 각주가 그 범위를 명시한다.

확정되지 않은 숫자·날짜는 넣지 않는다. 모자란 쪽이 낫다.

### 2.1 프로그램은 사이트에서 동등하다 (2026-09-03, Andrew 결정)

정본은 리코더 앙상블 과정을 "플래그십"으로 적어 두었지만, **사이트 표기는 동등하다.**
다섯 프로그램 어디에도 '대표·플래그십·main' 라벨을 붙이지 않는다.

- 홈: 다섯 카드가 **같은 크기·같은 항목·같은 순서**(`.grid-5` + `.prog`). 사진 크기도 같다.
- 프로그램 페이지: 다섯 개가 각각 **같은 구조의 블록**(사진 + eyebrow + 제목 + 리드 +
  본문 + `.facts` 5행)으로, 좌우 사진 위치만 번갈아 간다(`.split-flip`).
- 개요 표의 Status 칸에 "Flagship"을 다시 쓰지 않는다. 상태만 쓴다.
- 리코더 앙상블에만 있는 상세(파일럿, 12주 구성)는 프로그램 페이지의
  **"한 과정을 만드는 방식"** 섹션으로 옮겼다 — 그 섹션은 특정 프로그램이 아니라
  *배움 프로그램 전체의 설계 원칙*을 말한다. 그래서 `dg.term()` 도식이 거기에 있다.
- **파일럿은 홈에 올리지 않는다.** 숫자 띠의 "7/7 파일럿 수료"도 뺐다
  (지금은 40+ / 5 / 20+ / 4 / 143).

정본 문서를 고칠 일이 생기면 이 결정을 정본에도 반영할 것.

## 3. 공개 범위

이 사이트는 **대외 공개용**이다. 다음은 절대 올리지 않는다:

- 이민 신분·Stamp 1G·PAYE 철칙에 관한 모든 내용
- Grant 트래커, 신청 캘린더, 지원 결과 미확정 건(MIVA·Harrison Parrott 등)
- 개인 재정, 보험 증권번호, 상세 예산·단가표, 창립자 급여 구조
- 파트너 담당자의 개인 연락처
- 내부 전략 문서(11_전략마스터플랜 등)의 문장

법적 지위는 "비영리 CLG 설립 준비 중, 아직 등록 자선단체 아님"까지만 쓴다.
**기부금 세제 혜택이 있는 것처럼 쓰지 않는다.**

## 3.5 아키텍처 — 손대기 전에 읽을 것

이 사이트는 참조 저장소 두 곳의 구조를 그대로 가져왔다. 새 스타일을 즉흥으로 만들지 않는다.

**CSS 3층 구조 (LINE `abc-def` / `packages/styles`)**
`styles.css`는 ① primitives(정본 브랜드 hex, 여기서만 정의) ② semantic(`--surface`, `--fg`,
`--accent`, `--line` — 컴포넌트가 읽는 유일한 층) ③ component 순서다.
**컴포넌트에 hex를 직접 쓰지 않는다.** 색을 바꾸려면 semantic 토큰을 바꾼다.

**타이포 스케일은 토큰이다 — HTML에 font-size를 쓰지 않는다**
`:root`의 `--t-display / --t-h1 / --t-h2 / --t-h2-sm / --t-h3 / --t-lead / --t-body /
--t-small / --t-label` 아홉 개가 전부다. 필요하면 `.h-display` `.h-xl` `.h-lg` `.h-md`
클래스를 쓴다. **content_*.py에 `style="font-size:…"`를 새로 쓰지 않는다.**
간격도 마찬가지 — `.mt-1..4`, `.center`, `.center-row`가 있다.
`h1`–`h4`는 크기마다 자간·행간이 따로 잡혀 있고(EB Garamond은 올드스타일이라 자간을 덜 조인다),
한글은 `html[lang^="ko"]`에서 행간을 더 주고 자간을 덜 조인다. 한쪽만 고치지 않는다.

**모션 (SEED `packages/rootage`)**
`duration.yaml`·`timing-function.yaml`의 스케일을 `--dur-1..6`, `--ease*`로 옮겨 놨다.
새 duration/easing 값을 지어내지 말고 이 토큰을 쓴다.

- 스크롤 등장 = `animation-timeline: view()`, 진행바 = `scroll()`, 페이지 전환 =
  cross-document View Transitions. **전부 CSS다. 애니메이션용 JS·라이브러리를 추가하지 않는다.**
- `@view-transition`은 최상위에서만 유효하다. `@media` 안에 넣으면 조용히 무시된다.
  reduced-motion 대응은 `::view-transition-*` 의사요소로 한다.
- 모든 모션은 `@supports` + `prefers-reduced-motion: no-preference` 안에 둔다.
  미지원 브라우저에서는 그냥 정적으로 보여야 한다.
- 사이트 전체 JS는 모바일 메뉴 토글 한 줄뿐이다. 이 상태를 유지한다.

**네비게이션 — 프로그램은 하위 메뉴를 가진다**
`layout.py`의 `SUBNAV`가 다섯 프로그램 상세 페이지를 Programmes 항목 아래에 건다.
CSS만으로 여는 디스클로저다 — 마우스는 `:hover`, 키보드는 `:focus-within`.
**쉐브론(∨)을 다시 붙이지 않는다.** 글자 아래로 떨어져 nav 베이스라인을 깨뜨렸다.
**여기에 JS를 붙이지 않는다.** 880px 아래에서는 패널이 아니라 드로어 안의 들여쓴 목록이 된다
(터치에는 hover가 없으므로). 트리거와 패널 사이 12px 틈은 `.has-sub::after`가 메운다.

**눈썹(eyebrow) 앞에는 선을 두지 않는다**
`.eyebrow::before`로 그리던 30px 금색 선은 모든 제목 앞에 하이픈이 붙은 것처럼 읽혀서 뺐다.
라벨은 색과 자간만으로 선다. 되살리지 않는다.

**두 축은 카드 위를 가로지르는 띠로 보인다**
`.pillar-bar`는 `.grid-5`와 **같은 5열 그리드** 위에 놓여, `Learning`이 앞 세 장을,
`Sharing`이 뒤 두 장을 물리적으로 덮는다. 그래서 어느 카드가 어느 축인지 눈으로 보인다.
1040px 아래에서 카드가 3열로 접히면 띠는 자기가 가리키는 것과 어긋나므로 **사라진다** —
그때는 각 카드의 `.kicker`가 축을 말한다. 점 두 개짜리 옛 `.legend`로 되돌리지 않는다.

**설명은 문단이 아니라 도식으로**
한 섹션 본문은 50–80단어(한국어 두세 문장)가 상한이다. 그보다 길어지면 `_build/diagrams.py`의
도식으로 옮긴다. 도식은 **이름이 아니라 메커니즘**을 그린다 — 연결선에 라벨을 붙이고, 한 도식은
한 가지만 말한다.

**도식 어휘는 2026-09-07에 전면 교체했다 — 오선·음표로 되돌리지 않는다.**
Andrew의 판정: 오선 위의 음표 머리는 음악처럼 읽히기 전에 어수선하게 읽혔다. 지금의 어휘는
넷이고, 구조를 그리는 도식은 **SVG가 아니라 HTML/CSS**다(글자가 줄바꿈되고, 확대에 반응하고,
폰에서 세로로 다시 흐른다):

| 표시 | 클래스 | 뜻 |
|---|---|---|
| 패널 | `.fs` `.cv-item` `.cv-hub` `.loop-panel` | 이름 붙은 하나. `.is-claim`(점선)이면 셈이 아니라 주장, `.is-key`(금색)면 강조 |
| 점 | `.dot` / `.dot-open` | 채움 = 기록에 있음·배움, 빔 = 계획·나눔. **크기는 언제나 같다** |
| 연결선 | `.fs-join` `.cv-join` `.cmp-join`의 `::before/::after` (가는 금색 선 + 작은 머리), 라벨은 `.fs-edge` `.cv-edge` | 무엇이 무엇을 먹이는지 |
| 쐐기 | `.wedge` | "자란다"를 뜻하는 유일한 표시 |

레이아웃 부품은 넷 — `.flow`(패널 한 줄 + 연결선; 트랙 목록 `--flow-tracks`는 빌더가 준다), `.cv`(목록 → 금색 수집선 → 목적지;
`.cv-through`는 좌·우 양쪽), `.loop`(두 패널과 위아래 호), `.cmp`(두 패널 비교). 55em 아래에서는
전부 한 열이 되고 연결선이 아래로 꺾인다 — 그 미디어 블록을 지우면 폰에서 화살표가 옆으로 튄다.

SVG는 **데이터를 그리는 둘**뿐이다 — `chronicle`(해마다 선 하나, 활동마다 점 하나, 과정은 금색
막대)과 `attendance`(막대 그래프). 둘만 `.diagram-canvas`로 가로 스크롤한다. 도식은 열네 개
(`programme_map`=`loop`, `theory_of_change`, `term`, `subsidy`, `lecture_arc`, `outing`, `visit`,
`rehearsals`, `pathways`, `vocabulary`, `chronicle`, `attendance`, `roadmap`, `partnership`).
`vocabulary()`는 범례이고 `identity.html`에 있다. 새 도식을 만들 때는 위 넷 밖의 표시를 만들지
않는다.

- **연결선은 패널의 의사요소가 아니라 자기 그리드 트랙이다(2026-09-07 수정).** `.flow`는
  `minmax(0,1fr) auto minmax(0,1fr) …`로 패널과 `.fs-join`이 번갈아 오고, `.cv`·`.cmp`의
  `.cv-join`·`.cmp-join` 열도 `auto`다. 라벨(`.fs-edge` `.cv-edge`)은 절대배치가 아니라 그
  트랙 **안의 일반 흐름**에 있어서 틈이 라벨 폭만큼 넓어진다 — 전에는 라벨을 `z-index`와
  배경으로 이웃 패널 위에 얹었는데, 그게 "WHICHEVER FITS"가 목적지 제목을 가리던 사고의
  원인이었다. 데스크톱에서 라벨은 선 **위**에 앉고(`translate:0 calc(-50% - 6px)`, 배경 없음),
  55em 아래에서는 세로선 **위에** 배경을 깔고 앉는다. 라벨은 11em에서 줄바꿈하고 한국어는
  `word-break:keep-all`이다. **라벨을 다시 `position:absolute`로 빼지 않는다.**
- `.flow`의 각주 행(`.ff`)은 별도 `.flow-foot`이 아니라 **같은 그리드의 둘째 줄**이다 —
  `flow(steps, cols, foot=[…])`가 `--gc`(grid-column 시작/끝)를 계산해 넣는다. 조인 트랙이
  `auto`라 따로 떼어 놓으면 열이 어긋난다. 모션의 `.flow>.fs:nth-child(3/5/7)`도 조인이
  끼어 있어서 홀수다.
- 겹침 점검은 `scratchpad`의 프로브 방식이 빠르다: iframe으로 도식 있는 27페이지를 1240 ·
  1000 · 900 · 390px에서 열고 `.diagram` 안 텍스트 박스끼리의 교차 면적, 라벨과 패널의 교차,
  도식 밖으로 나간 텍스트, `nowrap` 잘림을 `getBoundingClientRect`로 센다.
- **`chronicle`의 데이터는 `_build/ledger.py` 한 곳이다.** 기록 페이지의 표(`_build/archive.py`)와
  성과 페이지의 숫자가 같은 목록을 읽으므로 행사를 더하거나 고칠 때 `ledger.py`만 고친다.
  `counts()`가 정본 §1의 합계(강의 17 · 2023–25 찾아가는 음악회 20 · 참석 143)와 맞는지 빌드 전에
  돌려 본다. 가까운 날짜의 점은 `CH_STACK`(17px)씩 위로 쌓이고, 막대는 `kind == "course"`에만 붙는다.
- **`roadmap`의 점선 넷은 계획이다.** 하나가 이루어지면 `done`으로 옮긴다. 계획에 날짜를 붙이지 않는다.

**경로는 `layout.py`가 붙인다 — content 파일은 전부 루트 기준으로 쓴다**
페이지가 `programmes/` 아래에도 있으므로 깊이가 두 종류다. `layout.py`에 접두사가 둘 있다:

- `_here(slug)` — **그 언어 트리의 꼭대기**로 돌아가는 경로. 페이지 사이 링크가 이걸 쓴다
  (`ko/about.html`이 한국어 소개 페이지이므로).
- `_root(lang, slug)` — **사이트 루트**로 돌아가는 경로. `images/`·`assets/`·`styles.css`처럼
  사본이 하나뿐인 파일이 이걸 쓴다.

content 파일에는 `href="programmes.html"`, `src="images/x.jpg"`처럼 **루트 기준으로만** 쓴다.
`../`를 직접 쓰지 않는다 — 둘을 헷갈리면 한국어 네비게이션이 영문 페이지를 가리키는 사고가 난다
(실제로 한 번 났다).

**프로그램 상세 5페이지는 렌더러 하나를 공유한다**
`_build/programmes_sub.py`가 뼈대를, `prog_en.py`/`prog_ko.py`가 문안을 갖는다.
다섯이 서로 다른 레이아웃을 가질 수 있게 되는 순간 하나가 '대표'처럼 보이기 시작하므로,
**섹션 순서·구조·`.facts` 행 수를 개별 페이지에서 바꾸지 않는다.** 바꾸려면 다섯 개 전부를
한꺼번에 바꾸는 렌더러 쪽을 고친다. 문안만 언어별로 다르고, 번역이 아니라 각 언어로 쓴 것이다.

**색 견본은 토큰으로 칠한다**
About 아이덴티티 섹션의 `.swatch`는 `style="--sw:var(--ink-navy)"`처럼 **primitive 토큰으로**
칠하고, 그 옆의 `<code>#1D2430</code>`은 문서화된 값을 글자로 적는다. 견본을 hex로 칠하면
토큰을 고쳤을 때 견본만 옛날 색으로 남는다. 둘이 어긋나 보이면 그건 버그가 눈에 띈 것이고,
견본이 해야 할 일이 바로 그것이다.

**사진은 한 가지 처리로 통일**
모든 이미지는 `.photo` 안에 고정 비율(`.photo-3x2` 등)로 넣는다. 공통 오버레이가 여러 방에서
찍은 사진을 한 세트로 묶어 준다. 이 래퍼 밖에 `<img>`를 두지 않는다.

**접근성 — 회귀시키지 말 것**
- 모든 본문/배경 조합이 WCAG AA(4.5:1, 큰 글자 3:1)를 통과한다. 색을 바꿨으면 다시 잰다.
- **금색을 글자에 쓸 때는 `--accent-ink`(#7F5C1C)다.** `--accent`(#B8893A)는 면·선·화살표용이고
  본문 크기 글자에 쓰면 대비가 2.9:1로 떨어진다. 로고는 어느 쪽으로도 건드리지 않는다.
- 금색 버튼(`.btn-accent`)의 글자색은 흰색이 아니라 잉크 네이비다(4.96:1).
- 어두운 밴드의 링크 규칙은 `a:not(.btn)`으로 걸어야 한다. 안 그러면 버튼 글자색을 덮어쓴다.
- 제목 레벨을 건너뛰지 않는다(h1→h2→h3). 섹션에 보이는 제목이 없으면 `.sr-only` h2를 넣는다.
- 라벨처럼 보여야 하는 제목은 `<h3 class="h-sub">`를 쓴다. `<h4>`를 쓰지 않는다.


**언어 전환은 헤더 맨 끝의 작은 한 쌍**
`.lang-switch`는 Contact 버튼 **뒤에**, 세로 헤어라인 다음에 온다. 채워진 알약 형태로 nav
한가운데 있던 때는 주요 액션만 한 무게를 가져 Contact와 경쟁했다. 지금은 11.5px 두 낱말이고
현재 언어에만 금색 밑줄이 붙는다. 현재 언어는 `<span aria-current="true">`, 다른 언어는
`<a>`라는 구조는 그대로다 — 이것을 일반 nav 링크로 되돌리지 않는다.

**카드 안에 떠 있는 화살표를 두지 않는다**
`.prog` 카드 오른쪽 아래 구석에 있던 `→`는 어디에도 붙어 있지 않은 글리프처럼 보였다.
링크라는 신호는 hover 시 제목이 금색으로 바뀌는 것과 카드가 떠오르는 것으로 충분하다.

**네비게이션은 6 + Contact다 (2026-09-07)**
About · Programmes · Get Involved · What's On · Impact · Support + Contact 버튼. 정본
`12_브랜드웹마스터` §8이 조사한 27개 예술단체의 상한(5–6)에 맞췄다. 여섯 중 다섯이 패널을
연다(`SUBNAV`) — About(단체 소개 · 창립자 · 상징과 표준), Programmes(5), What's On(다가오는 일정 ·
기록), Impact(달라진 것 · 가려는 곳 · 투명성), Support(후원하기 · 기관 파트너십).
**일곱 번째 항목을 더하지 않는다.** 새 페이지는 이 다섯 패널 중 하나 아래에 건다.
드로어(880px 아래)는 모든 하위 항목을 펼쳐 보이므로 `max-height:calc(100dvh - 74px)` +
`overflow-y:auto`로 자기 안에서 스크롤한다 — 이 두 줄을 지우면 긴 드로어의 아랫부분이
잘린다. 하위 페이지(`identity` `archive` `partner`)에서 부모 항목이 켜지는 것은 `layout.py`
`header()`가 `SUBNAV`의 href를 보고 판단한다.

**날짜 있는 숫자 타일(`.figures`)은 CSS만으로 센다**
`--c`를 `@property`로 정수 등록하고 `counter-reset`으로 찍는다. 목표값은 마크업의
`style="--n:143"`이고, 실제 숫자도 `<span>`으로 마크업에 있다 — 지원하지 않는 브라우저·
reduced-motion·스크린리더는 그 span을 읽는다. **타일마다 기간(`.figure-period`)이 반드시
붙는다.** 기간 없는 숫자는 만들지 않는다(정본 §10-3).

**신뢰 띠(`.ticker`)는 로고가 아니라 이름이다.** 어느 기관의 로고도 게재 허락을 받지 않았다.
두 번째 `<ul>`은 `aria-hidden`이고 reduced-motion에서는 한 목록만 줄바꿈으로 보인다.

**FAQ는 `<details>`다.** JS 없이 여닫는다. 같은 네 문답이 `build.py`의 `FAQ`에도 있어
`FAQPage` 구조화 데이터로 나간다 — **한쪽만 고치지 않는다.**

## 3.6 홈페이지 순서 — 이유가 있다

동종 단체(Music and Health Ireland, Live Music Now, 함께하는 사랑밭)의 공통 패턴을 따랐다.
**방문자에게 무엇을 부탁하기 전에 단체가 무엇을 운영하는지를 먼저 말한다.**

1. 히어로 — `for Everyone`의 **E는 대문자**다. 로고의 비례가 말하는 것과 히어로가
   어긋나면 안 된다.
2. ~~숫자 띠~~ — **뺐다(2026-09-04, Andrew 결정).** 무엇을 하는 곳인지 말하기도 전에
   40+ / 5 / 20+ / 4 / 143을 먼저 세면 하는 일이 아니라 실적표처럼 읽힌다. 같은 숫자는
   근거로 쓰이는 자리에 그대로 있다 — 프로그램 카드의 `.meta`, About, 그리고 아래 8번
   "신뢰의 근거". **홈 상단으로 되돌리지 않는다.**
3. **우리가 하는 일** — `.pillar-bar`(두 축 띠) + 다섯 카드 + `dg.loop()` 전체 지도
4. **지금 열려 있는 것** — 모집 중인 수업 / 다가오는 음악회
5. **왜 이 일을 하는가** — `.band-photo`
5b. **이야기 하나** (2026-09-07) — 2024년 11월 성 골롬반 수녀회 음악회에서 나온 "다시
   연주할 수 있을까요"라는 질문이 파일럿과 Mulhuddart 과정이 된 이야기. 정본 `09` §2의
   "an audience became players"를 그대로 옮긴 것이고, **수녀의 이름은 동의를 받지 않았으므로
   적지 않는다.** 이야기 블록은 이 하나뿐이다 — 동의서 없는 인용은 올리지 않는다.
6. **이름이 지우는 책임** — `.band-mark`. 로고를 실물 크기로 한 번 보여 주고,
   금색이 왜 `Everyone` 위에 놓이는지를 말한다. 마스터 태그라인이 여기 있고,
   `identity.html`로 연결된다.
7. **두 가지 목표** — 접근성, 그리고 음악교육자의 정규 고용. 두 번째를 흐리지 않는다.
   같은 섹션 아래에 **가려는 곳**(`.next-list`, 마침 1행 + 계획 4행)이 붙어 `impact.html#next`로
   간다. 계획 행에 날짜를 넣지 않는다.
8. **후원이 이미 바꾼 것** — `.evidence` 3칸(후원으로 마련한 차량 · 그래서 닿은 곳 ·
   공적 위촉 한 건) + 추천사 + **신뢰 띠**(`.ticker`, 연주한 기관 이름). 2026-09-04에
   "신뢰의 근거"에서 이 방향으로 바꿨다: 보증서를 늘어놓는 대신, 후원이 실제로 무엇을
   가능하게 했는지를 말한다.
8b. **기관 초대** — "요양시설이나 병원, 본당이신가요?" + 2분 문의(본문이 미리 적힌 mailto).
   정본 §10-8이 "사실상 영업"이라고 부른 블록이다. `partner.html`로 간다.
9. 참여·후원 CTA

번호는 옛 순서를 그대로 두고 2번만 비워 두었다. 다시 채우지 말 것.
이 순서를 바꾸려면 두 언어를 같이 바꾼다. 3번을 4번 뒤로 미루지 않는다 —
사이트가 다시 "수업 하나 파는 곳"처럼 읽힌다. 6·7번은 5번(감정) 다음, 8번(증거) 앞에
있어야 한다 — 왜 → 무엇을 약속하는가 → 근거 → 부탁 순서다.

## 3.7 참조한 디자인 시스템 여덟 곳 — 무엇을 가져왔나 (2026-09-04)

Andrew가 지정한 여덟 곳을 읽고, **이 사이트에 실제로 적용되는 것만** 가져왔다.
컴포넌트 라이브러리를 흉내 내지 않았다 — 이 사이트는 마케팅 사이트지 앱이 아니다.

| 시스템 | 가져온 규칙 | 어디에 |
|---|---|---|
| **Adobe Spectrum** | 토큰 이름은 `맥락 → 공통 단위 → 구체화` 순서. **primitive를 용도로 이름 붙이지 않는다** | `--gold-text` → `--gold-deep`으로 개명. 용도 이름(`--accent-ink`)은 semantic 층에만 |
| **Spectrum** | 포커스 표시는 **두 겹**이어야 어떤 배경에서도 보인다 | `:focus-visible`이 밝은 헤일로 + 어두운 링. 어두운 밴드에서 둘이 뒤바뀐다 |
| **SEED** | 폰트 크기는 **rem** (`rootage/font-size.yaml`) | `--t-*` 아홉 개 전부 rem. px로 되돌리지 않는다 |
| **SEED** | 간격은 4의 배수 (`dimension.yaml`) | `.mt-1..4` = 12/20/28/40 |
| **Fluent 2** | 그림자는 **key(선명·아래로) + ambient(부드러움·방향 없음)** 한 쌍. blur가 배가되고 y offset은 blur의 절반 | `--shadow-1/2/3` |
| **Fluent 2** | 모든 컨트롤이 rest / hover / **pressed** / focus를 정의한다 | 모든 `.btn`에 `:active` |
| **Fluent 2** | **비텍스트 요소도 인접색 대비 3:1** (WCAG 1.4.11) | `--line-interactive` 신설. `--line-strong`은 크림 위에서 1.5:1이라 컨트롤 테두리로 쓰면 안 된다 |
| **Fluent 2** | 320px까지 가로 스크롤 없이, 200% 텍스트 확대에서 잘림 없이 | 브레이크포인트를 **em**으로, `@media (max-width:23.75em)` 추가, 헤더가 넘치지 않고 줄바꿈 |
| **Gestalt · TDS** | 터치 타깃 최소 44px | `.btn{min-height:46px}`, 드로어의 nav·서브메뉴·언어전환 |
| **abc-def** | primitives → semantic → component 3층 | 이미 그 구조였다. 유지 |
| **Montage · Astryx** | 새로 가져온 규칙 없음 — 앞의 것들과 겹치거나 컴포넌트 라이브러리 영역이다 | — |

**되돌리면 안 되는 것 세 가지**

1. **px 폰트 크기로 돌아가지 않는다.** 브라우저 기본 글자 크기를 키운 독자를 무시하게 된다.
   `.dg-*`(도식 안 글자)만 예외 — 그건 viewBox 단위라 SVG와 함께 커진다.
2. **브레이크포인트를 px로 되돌리지 않는다.** em이라야 텍스트 확대에 반응한다.
3. **컨트롤 테두리에 `--line-strong`을 쓰지 않는다.** 장식용 테두리는 `--line`,
   버튼·토글처럼 **누르는 것**의 테두리는 `--line-interactive`다.

## 3.8 디자인 규율 — 여기서 어긋나면 티가 난다 (2026-09-04)

§3.7의 원칙을 실제 CSS에 적용하면서 드러난 것들. **이 셋을 되돌리지 않는다.**

**작은 라벨은 한 가지 조리법이다**
`.eyebrow` `.kicker` `.h-sub` `.tag` `.evidence dt` `.tl-date` `.swatch b` `thead th` …
열세 개가 제각기 크기 다섯 가지, 자간 여덟 가지(.05 / .09 / .11 / .13 / .14 / .15 / .17 / .18em)를
쓰고 있었다. 어느 하나도 틀리지 않았는데 **어느 둘도 서로 일치하지 않아서** 페이지가
덜 다듬어진 것처럼 읽혔다. 지금은 `styles.css` §3의 선택자 목록 하나가 조리법을 정하고,
컴포넌트 규칙은 **색·여백·레이아웃만** 정한다. 크기는 `--t-label` 하나, 좁은 자리에만
`--t-label-sm`. 자간은 `--label-track`(한글 `--label-track-kr`) 하나.
**새 라벨을 만들면 그 선택자 목록에 추가한다. 새 자간을 지어내지 않는다.**

**컴포넌트 안쪽 여백은 세 단계뿐이다**
8 / 18 / 22 / 24 / 26 / 28 / 30 / 34로 흩어져 있던 값을 `--pad-sm`(20) · `--pad`(26) ·
`--pad-lg`(34)로 모았다. 전부 4의 배수다. 반지름도 6 / 12 / 20(SEED r1_5·r3·r5).

**그림자는 뜻이 있다 — 아무 데나 붙이지 않는다**
Fluent의 모델대로 그림자는 "페이지에서 얼마나 떠 있는가"이고, 곧 중요도다.
전에는 도식·로고 판·카드·공지가 전부 같은 그림자를 달고 있어서 **아무것도 떠 보이지 않았다.**

| 층 | 무엇 | 값 |
|---|---|---|
| 평평 | 도식, 로고 판, 표, 색 견본 — **읽는 것** | 그림자 없음, 1px 선으로만 |
| 1 | 카드(`.card` `.prog` `.notice` `.exchange` `.pillar`) — **무언가를 내미는 것** | `--shadow-1` |
| 2 | 그 카드가 포인터에 답할 때 | `--shadow-2` |
| 3 | 유일한 오버레이, Programmes 패널 | `--shadow-3` |

첫째 층에 그림자를 붙이는 순간 다시 전부 평평해 보인다.

## 3.9 구조화 데이터 — `@graph` 하나로 낸다

페이지마다 Organization 블록을 25번 복사하던 것을 **`@graph`**로 바꿨다.
단체는 `#organisation` id로 한 번만 선언하고, 나머지 노드(WebSite · WebPage ·
BreadcrumbList · Event)는 그 id를 **가리키기만** 한다.

- `_build/layout.py`의 `ORG_NODE` / `SITE_NODE` / `PAGE_NODE` / `breadcrumb()`가 뼈대,
  `_build/build.py`의 `EVENTS` / `OG` / `extra_nodes()`가 페이지별 추가분이다.
- **행사는 `EVENTS`와 content 파일의 `WHATS_ON`이 같은 사실을 말해야 한다.**
  날짜·장소·시간을 한쪽만 고치지 않는다.
- **`nonprofitStatus`를 넣지 않는다.** schema.org가 정의한 값은 전부 특정 국가 제도
  (미국 501c3, 네덜란드 ANBI 등)이고 어느 것도 해당하지 않는다. 아직 등록 자선단체도
  아니다(§3).
- OG 이미지는 `OG` 표가 페이지마다 다르게 준다. 전 페이지가 히어로 한 장을 공유하지
  않는다 — 프로그램 링크를 공유하면 그 프로그램 사진이 나와야 한다.

## 3.9.5 가독성 — 글보다 먼저 보이는 것 (2026-09-07)

Andrew의 두 번째 지적: 글이 너무 많고 핵심이 안 보인다. 그래서 문단을 하나 더 쓰기 전에
쓰는 장치가 넷이다(`styles.css` §21).

- **`.statement`** — 문장 하나를 제목 크기로, 핵심 구절만 `<em>`(금색 이탤릭, 한글은 정체).
  미션·비전·파일럿 요약이 이걸 쓴다. 한 섹션에 하나.
- **`.glance`** — 라벨 위 값, 4칸. 페이지 첫 화면 바로 아래에 "한눈에".
- **`.values`** — 번호·낱말·한 줄. 가치 다섯을 카드 여섯 장 대신 한 줄로.
- **`<details class="faq">`** — 참고용 목록(이력, FAQ)은 접어 둔다.

프로그램 페이지의 블록 본문은 **한 문장**, 소식의 카드 본문도 **한 문장**, 기록의 이정표는
**한 줄**이다. 다시 늘리지 않는다. 늘려야 할 사실이 있으면 `.facts` 행이나 도식으로 간다.

## 3.10 네 페이지가 2026-09-07에 늘었다 — 무엇이 어디 있나

| 페이지 | 무엇 | 정본 출처 | 지키는 것 |
|---|---|---|---|
| `impact.html` | 날짜 있는 숫자 8개 · 달라진 것 8항 · 참석 추이 도식 · 이야기 · 변화 이론 · 가는 길(`#next`) · 투명성(`#transparency`: 지위 · 증명 못 한 것 5항 · 지원 내역 표 · 서한) | `03` §1–2, `09` §2–6, `05` §1 | 웰빙·고립에 관한 주장 금지. 지원 내역 표는 **여기 한 곳**에만 있다(후원 페이지는 링크만). SDCC €2,000은 정본이 공개하라고 한 액수라 적었고, 그 밖의 예산·단가는 적지 않는다 |
| `archive.html` | 연대기 도식 · 지나온 길 연표(소식에서 옮김) · 해마다 표 · 계획했지만 못 한 것 4건 · 아직 기록되지 않은 것 · 기록하는 법 | `03` §3, `08` §6–8 | 행은 `ledger.py`에서만 온다. 아마추어 단원·참가자의 이름은 적지 않는다(동의 없음). "못 한 것"은 정본 §7의 표현대로 실패 대신 축소로 쓴다 |
| `partner.html` | 받는 것 → 세 가지 길 도식 → 방을 맡은 곳(`visit` 도식 + 3종 카드) → 함께한 기관 이름 + 숫자 4개 → FAQ 4 → 네 줄 메일 | `12` §11, `09` §4 | 후원 등급·금액을 적지 않는다(정본의 등급표는 제안이지 확정이 아니다). 세제 혜택 없음을 FAQ에서 먼저 말한다 |
| `identity.html` | 로고 밴드 · 로고 3판 · 사용 규칙 · 색 견본 · 서체 · 도식 범례 · 이름 적는 법 · 마스터 라인 | `06`, `01` §7 | About의 옛 `#identity` 섹션을 통째로 옮겼다. About에는 안내 블록(`id="identity"` 유지)만 남는다 |
| `founder.html` (2026-09-07) | **아티스트 프로필**: 세로 초상(`founder-portrait.jpg`) + 이름 + 한 줄 + 인용 한 문장 + 소셜 링크 → "일하는 방식" + 사실 6행 → 두 번째 사진과 실천의 뿌리 → 함께하는 예술가 3 → **이력은 `<details>` 아코디언 5개** → 편지하기 | `04` §1–2·9 | About에서 CV처럼 늘어져 있던 학력·역할·자원봉사 목록을 전부 여기 아코디언으로 옮겼다. 소셜 링크는 `layout.py`의 `SOCIAL`에 URL이 있을 때만 그려진다 — **핸들을 지어내지 않는다.** 인스타그램은 비어 있다(§7) |

**About은 2026-09-07에 절반으로 줄였다.** 순서: 세 문장 조직 이야기(정본 `12` §1의 EN판) →
`.glance` 4칸 → 미션·비전을 `.statement`로 → `.values` 한 줄 5개 → `loop` 도식 → 연주한 곳
이름 구름(`.names`) → 창립자 티저(→ `founder.html`) → 아이덴티티 티저(→ `identity.html`).
변화 이론 도식은 성과 페이지에만 있다. **About에 목록형 이력을 되돌리지 않는다.**
About 패널의 세 항목(단체 소개 · 창립자 · 상징과 표준)은 이제 각각 페이지다.

- 소식 페이지(`news.html`)는 이제 **다가오는 일정 + 올해 + 기사**만 갖고, 연표는 기록
  페이지로 갔다. 네비 라벨은 "What's On / 일정과 소식"이지만 파일명은 바꾸지 않았다(링크 유지).
- 후원 페이지는 **후원이 한 일 → 후원금이 되는 것(4카드) → 교차보조 도식 → 네 가지 방법 →
  투명성 링크** 순서다. "후원금이 되는 것"에 단가는 리코더 값(€8–15, 정본에 있음)만 있다.
- 구조화 데이터: 창립자 `Person` 노드(LinkedIn `sameAs` 포함, 정본 `04` §2), 모든 내부
  페이지의 `BreadcrumbList`, `partner`의 `FAQPage`, `programmes`의 `ItemList`. `llms.txt`와
  `site.webmanifest`도 빌드가 만든다. **매니페스트의 `short_name: "CMFE"`는 §6.5의 예외다**
  (홈 화면 아이콘 아래 12자 제한).
- 정본 `08` §2는 BBC 프롬스를 2023년과 2025년으로 적고 있어 "두 해 연속(consecutive)"은 사실이
  아니었다. 두 언어 모두 **"두 번의 여름 / two summers"**로 고쳤다. 정본 `03` §2의
  "two consecutive summers"도 갱신 대상이다(§7).

## 4. 파일 구조

- 영문 17페이지는 루트, 한국어 17페이지는 `ko/`. 두 언어는 항상 같이 고친다.
  (기본 7 + `programmes/` 아래 프로그램별 상세 5 + `impact` `archive` `partner` `identity` `founder`)
- 반복되는 부분(헤더·네비·푸터·`<head>`)은 `_build/`에서 고치고
  `python3 _build/build.py`로 34페이지 + 404 + sitemap + robots + llms.txt + manifest를 다시 만든다.
- **화면 확인은 헤드리스 크롬으로 한다.** 데스크톱 앱의 브라우저 패널이 숨겨져 있으면
  스크롤 캡처가 빈 화면으로 나온다. `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
  --headless=new --allow-file-access-from-files --window-size=1240,6400 --virtual-time-budget=8000
  --screenshot=out.png file:///…/index.html`이 확실하다. 가로 넘침은 iframe으로 `scrollWidth`를
  재는 `--dump-dom` 프로브가 빠르다(2026-09-07에 12페이지 모두 360px에서 넘침 없음 확인).
- `404.html`은 어떤 깊이의 잘못된 주소에서도 뜨므로 링크·에셋이 전부 절대경로다.
  로컬 확인은 상위 폴더에서 서버를 띄우고 `/classicalmusicforeveryone/404.html`로 연다.
- `_build/`는 `_` 로 시작하므로 GitHub Pages가 발행하지 않는다.
  **`.nojekyll` 파일을 만들지 말 것** — 만들면 `_build/`가 공개된다.
- `styles.css`·`assets/`·`images/`는 빌드가 건드리지 않는다. 직접 관리한다.
- **사진 경로는 두 언어 모두 `src="images/…"`로 쓴다.** 한국어 페이지는 `ko/` 안에 있어
  `../images/`가 필요한데, 그 접두사는 `_build/layout.py`의 `_images()`가 붙인다.
  content 파일에 `../images/`를 직접 쓰지 않는다 — 한쪽만 틀리는 사고가 여기서 났다.
- 도식은 `_build/diagrams.py`에서 두 언어 라벨을 함께 관리한다. HTML에 SVG를 직접 붙여넣지 않는다.
- 로고는 `10_CMFE_로고·브랜드셋업/`에서만 가져온다. `03_CMFE_브랜드/`는 아카이브다.

## 5. 사진

**정본은 `images/README.md`다.** 어떤 사진이 어디 쓰이는지, 비율 클래스가 무엇인지 거기 있다.

- 새 사진: `_build/add-photo.sh <원본> images/<이름>.jpg` — 1400px·품질 68로 줄이고
  붙여넣을 HTML까지 출력해 준다. 한 장 500KB 이하.
- `loading`·`decoding`·`fetchpriority`는 빌드가 자동으로 넣는다. **손으로 쓰지 않는다.**
- 사진은 반드시 `.photo` 래퍼 안에. 밖에 `<img>`를 두면 처리가 어긋난다.
- 참가자·관객의 얼굴이 식별되는 사진은 **동의가 확인된 것만** 쓴다. 확실하지 않으면 쓰지 않는다.
- 캡션에 확인되지 않은 행사명·장소를 쓰지 않는다.

## 6. 글쓰기

- 영문은 아일랜드/영국 철자: organisation, programme, centre, whilst→while.
- 과장 형용사·감탄사·em-dash 남용 금지. 정본의 "ready-to-paste" 문장이 있으면 그대로 쓴다.
- 한국어판은 영문의 번역이 아니라 같은 사실을 한국어로 쓴 것이다.
  프로그램 이름은 정본 `01_단체개요_KR`의 표기를 따른다
  (클래식 음악과 친해지기 / 함께하는 음악여행 / **무료 리코더 앙상블 과정** / 찾아가는 음악회).
  **2026-09-07, Andrew 결정: 리코더 과정은 두 언어 모두 이름 앞에 "무료 / Free"를 붙인다**
  (`Free Recorder Ensemble course`). 정본 `01`·`02`의 표기는 아직 옛 이름이므로 갱신 대상이다(§7).
- 표는 반드시 `.table-scroll` 안에 넣는다. 본문이 가로로 스크롤되면 안 된다.

## 6.5 이름과 태그라인

- 단체명은 **`Classical Music for Everyone`** 한 가지 모양이다. 전부 대문자로 쓰지 않고,
  대외 문안에서 `CMFE`로 줄이지 않는다. 본문 안에 넣을 때는 `<span class="brandname">`으로
  감싼다 — 그 클래스가 `text-transform`을 막고 줄바꿈을 잡아 준다.
- **2026-09-04에 이 규칙을 어긴 곳 네 군데를 찾아 고쳤다** — `news`의 연혁과 `support`의
  안내 문구가 두 언어 모두에서 "CMFE"로 줄여 쓰고 있었고, `subsidy` 도식의 한국어
  `aria-label`도 그랬다. 소개 페이지가 "대외 문안에서 CMFE로 줄이지 않는다"고 적어 둔
  바로 그 사이트에서 났다. **JSON-LD의 `alternateName`과 mailto의 subject는 예외다** —
  구조화 데이터와 메일 제목이지 읽는 문안이 아니다.
- 마스터 태그라인은 **"Bringing classical music where it's needed!"**(한국어 내부용 대역:
  "클래식 음악을, 그것이 필요한 곳으로!")이고 모든 페이지 푸터에 `.footer-line`으로 들어간다.
  **느낌표로 끝난다**(2026-09-04, Andrew 결정 — 그 전까지는 마침표였다). 이전 기본
  태그라인("Making live classical music a shared, everyday experience")은 2026-09-03에
  폐기됐다 — 되살리지 않는다.

## 6.6 한국어 문체 — 번역체를 남기지 않는다

한국어판은 영문의 번역이 아니다(§6). 실제로 걸렸던 패턴들:

- **영어의 왼쪽 가지치기를 그대로 옮기지 않는다.** "…한 사람들을 위한 들어오는 길입니다"처럼
  긴 수식이 명사 앞에 쌓이면 한국어로는 읽히지 않는다. 문장을 끊는다 —
  "모자란 것은 길입니다. 나이가 많거나, 몸이 아프거나, … 사람이 들어설 길."
- **추상명사로 끝내지 않는다.** "가르치고 … 찾아감으로써" 같은 부사절 꼬리, "…하는 것입니다"의
  반복, "~을 통해"의 남용을 피한다.
- **영어 단어를 직역한 비유를 쓰지 않는다.** circuit → "회로"(X) / "한 바퀴"(O),
  way in → "들어오는 길"(X) / "들어설 길"(O), brief → "과업"(X) / "해야 할 일"(O).
- 나열은 가운뎃점보다 조사로 푸는 편이 자연스럽다 — "참여·지속·증언이지" →
  "참여와 지속과 증언이지".

**부정 병렬("X가 아니라 Y")을 사이트의 말버릇으로 만들지 않는다 (2026-09-04)**
한국어판 전체에 이 구문이 스무 번 넘게 있었다. 하나하나는 멀쩡한데 **사이트 전체가 같은
수사만 반복해서** 사람이 쓴 글이 아니라 생성된 글처럼 읽혔다. 대부분을 직접 진술로 바꿨고,
값을 확실히 치르는 서너 개만 남겼다(예: "신청서 네 종류가 아니라, 문장 네 개입니다",
"형편이 나아지면 그때 하겠다는 약속이 아닙니다"). **새 문안을 쓸 때 이 구문을 또 늘리지
않는다.** 거부한 절반을 지우고 긍정 절반을 구체적으로 다시 쓰는 쪽이 거의 언제나 낫다.

**직역 어휘를 되살리지 않는다.** 실제로 걷어낸 것들:
`존엄이 돌아온다`(dignity returns) · `~을 가로질러`(across) · `의미는 지속에서 자란다` ·
`현존의 봉사`(ministry of presence) · `음악을 반쯤 마중 나오도록`(meet halfway) ·
`같은 계산입니다`(the same arithmetic) · `저희를 붙잡을 수 있도록`(hold us to it) ·
`무엇이 되어야 하는가` · `실연`(→ **실황 연주**) · `대장(帳)으로 남긴다`.

**본문 안의 줄표(—)는 마침표로 끊는다.** 목록의 `제목 — 설명` 구분자는 그대로 둔다.

고칠 때는 영문도 같이 본다. 한쪽만 고치면 두 판본이 다른 말을 하게 된다.

## 7. 알려진 후속 과제

- ~~인스타그램 주소~~ — 받았다(2026-09-07): `instagram.com/sh.andrew_ryan`. `_build/layout.py`의
  `SOCIAL`에 있고, 창립자 페이지 버튼과 Person 노드의 `sameAs`에 자동으로 들어간다.
- **정본 `01`·`02`의 프로그램명을 "무료 리코더 앙상블 과정 / Free Recorder Ensemble course"로
  갱신해야 한다**(2026-09-07 결정).
- 창립자 초상 두 장(`founder-portrait.jpg` = IMG_1095, `founder-speaking.jpg` = IMG_8289)은
  `영상, 사진 아카이브/대표사진`에서 가져왔다. 다른 사진으로 바꾸려면 `_build/add-photo.sh`로
  같은 파일명에 덮어쓴다 — 세로(4:5) 사진이어야 창립자 히어로가 맞다.

- **정본 `03` §2의 "BBC Proms in two consecutive summers"를 "two summers"로 고쳐야 한다.**
  같은 정본 `08` §2가 2023년과 2025년으로 적고 있다(2026-09-07 발견).
- **파트너·후원 페이지의 다음 단계는 결제 링크다.** 정본 `12` §13.1대로 자체 결제를 만들지 않고
  GoFundMe(지금) → iDonate(자선 등록 후)로 연결한다. 링크 주소를 Andrew에게 받아 `support.html`의
  "후원금" 카드 버튼으로 넣는다.
- **`impact.html`의 "웰빙 측정" 약속은 2026년 가을 기수의 사전·사후 조사가 전제다.** 조사를
  실제로 시작하지 않으면 그 카드 문안을 다시 본다.
- 정본 `12` §3.2가 권한 접근성 툴바(대비·글자 크기)는 JS가 필요해 넣지 않았다. 사이트의
  "JS는 메뉴 토글 한 줄" 원칙을 바꾸기로 하면 그때 검토한다.

- **정본을 두 곳 갱신해야 한다(2026-09-04).** ① `06_Brand-Standards_EN`의 제목 서체가
  아직 Fraunces다 — EB Garamond으로. ② `00_최종본`의 차량 항목이 아직 "미확보"다 —
  확보로. 사이트가 이미 둘 다 바뀐 상태이므로 정본이 뒤처져 있다.
- **차량의 모금액·시기·기여자 수를 Andrew에게 받아야 한다.** 받기 전까지 홈의 문장은
  숫자 없이 사실만 말한다.

- 정본 `06_Brand-Standards_EN` §4·§6의 "웹사이트 주소를 절대 쓰지 말 것 — 웹사이트가 없다"는
  이제 사실이 아니다. 사이트 공개 후 정본과 이메일 서명·포스터를 갱신해야 한다.
- 홈 히어로 사진 캡션(성 파트리치오 축일 음악회)의 정확한 행사·장소를 Andrew가 확인해야 한다.
- 자체 도메인을 붙이면 `_build/layout.py`의 `SITE_URL`을 바꾸고 다시 빌드한다.
- 정본 `04_Founder-Profile_EN` §10은 Garda 신원조회가 **아직 미완료**라고 적고 있다.
  사이트 문안은 "필요한 일은 시작 전에 마친다"로 맞춰 놓았다. 완료되면 문안을 다시 본다.
- 참고한 동종 사이트: musicandhealthireland.ie, livemusicnow.org.uk — 섹션당 50–75단어,
  히어로 직후 숫자, 섹션마다 버튼 하나라는 패턴을 여기서 가져왔다.
